# GitHub Public Beta Release

The Package 8O release target is:

- Tag: `v0.2.0-public-beta`
- Title: `AI Objective Index v0.2.0 Public Beta`
- Type: prerelease

Release notes must say:

- AOI is read-only.
- `public_beta_mcp` contains Official MCP Registry metadata candidates.
- Candidates are not verified, not security certified, not a quality guarantee, and not purchasing advice.
- AOI does not perform payment, booking, login, email sending, form submission, purchase, contract signing, account connection, supplier claim, or supplier verification.

The release manager does not use `--clobber`, does not delete releases, and does not force push.
