# Feedback Reply Second-Run Candidate

A second-run candidate is created only when the reply is local, redacted, consent-bounded, and asks for a local second pass.

Allowed operations are limited to local redacted review, local receipt regeneration, and local feedback memory update.

Forbidden operations include GitHub API calls, external posting, external repo mutation, merge, deploy, publish, live MCP calls, data upload, and model training.
