# Negative Control Probes

Negative controls are known-bad local fixtures used to check whether the probe layer blocks unsupported claims.

The first 9E negative control contains unsupported positive claim wording and should produce `BLOCK_UNSUPPORTED_CLAIM`.

The false pass count must remain `0`. A false pass indicates the probe validator needs repair before stronger use.
