# ObjectiveCard

ObjectiveCard captures what an agent is trying to do before AOI routes to capabilities.

It includes task, domain, agent role, desired output, risk level, budget, latency, environment, allowed actions, forbidden actions, evidence requirement, and claim ceiling.

ObjectiveCard is a routing input, not a permission grant. Forbidden actions remain forbidden even when a capability appears to fit.
