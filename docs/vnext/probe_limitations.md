# Probe Limitations

Package 9E probes are local metadata probes only.

They are not external validation, not security certification, not a quality guarantee, not legal/safety/product readiness, not purchasing advice, and not action authorization.

Metadata may be stale or incomplete. Source traces can be partial. A local probe pass only says the local fixture check did not find a configured problem.
