# Evals

Future AOI eval harnesses will live here.

Initial eval ideas:

- validate that golden queries produce bounded recommendations;
- check that missing fields are surfaced;
- check that source trace coverage affects score explanations;
- check that forbidden actions are refused or excluded;
- check that decision receipts remain explanatory and read-only.

Golden query seeds live in `data/golden_queries.json`.

